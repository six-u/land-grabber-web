// 位置接口
export interface iPosition {
  x: number // X坐标
  y: number // Y坐标
}

// 大小接口
export interface iSize {
  width: string // 宽度
  height: string // 高度
}

// 索引接口
export interface iIndex {
  idx: string // 索引
}